// Manus Forge Maps proxy — removed. Stub preserved for import compatibility.
export async function makeRequest<T>(_endpoint: string, _params?: Record<string, unknown>): Promise<T> {
  throw new Error("makeRequest (Maps): not available in self-hosted mode");
}
